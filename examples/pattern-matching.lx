# Pattern matching on values and lists
let describe = (x) {
    0 => "zero"
    1 => "one"
    _ => "other"
};

let sum = (xs) {
    [] => 0
    [x, ...rest] => x + sum(rest)
};

let result = sum([1, 2, 3, 4]);
describe(result)
