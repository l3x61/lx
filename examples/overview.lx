let fizzbuzz = (n) {
    ? n % 15 == 0 => print("FizzBuzz")
    ? n % 3 == 0 => print("Fizz")
    ? n % 5 == 0 => print("Buzz")
    => print(n)
};

let loop = (i, max) {
    i, max ? i > max => print("Done.")
    => {
        fizzbuzz(i);
        loop(i + 1, max)
    }
};

loop(1, 15)
